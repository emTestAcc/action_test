on:
push:
branches:
- main
# for when updates to specific parts of the  project should not trigger an action:
#paths-ignore:
#    - 'docs/**'


jobs:
  job1:
    runs-on: ubuntu-latest
    shell: bash
    # Map a step output to a job output
    outputs:
      output1: ${{ steps.step1.outputs.test }}
     steps:
    - id: step1
      run: /create_image.sh
